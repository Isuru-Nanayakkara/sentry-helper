name: sentry_demo
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  sentry_flutter: ^6.2.2
  package_info: ^2.0.2
  device_info: ^2.0.3
  platform_info: ^3.1.0
  connectivity: ^3.0.6
  system_info: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true